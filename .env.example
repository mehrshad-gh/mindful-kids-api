# Railway sets DATABASE_URL when you add Postgres and reference it.
# Only set these if you need to run the API elsewhere (e.g. one-off script).

NODE_ENV=production
JWT_SECRET=your-super-secret-key-change-in-production
JWT_EXPIRES_IN=7d
